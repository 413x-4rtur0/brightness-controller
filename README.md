# Brightness Controller

GTK brightness control UI for Linux.

## Features
- Range: 10% to 100%
- Fine adjustment: 0.25% steps
- Uses `brightnessctl` + `sysfs`

## Usage
```bash
python3 brightness-gui.py
```
